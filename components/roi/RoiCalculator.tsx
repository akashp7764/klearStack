// components/roi/RoiCalculator.tsx — STUB (replaced in Phase 04)
export default function RoiCalculator() {
  return (
    <section
      id="roi"
      aria-labelledby="roi-section-title"
      style={{
        background: "var(--color-surface)",
        padding: "5rem 2rem",
        minHeight: "400px",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <div style={{ textAlign: "center", color: "var(--color-text-muted)" }}>
        <h2
          id="roi-section-title"
          style={{ fontSize: "1.5rem", fontWeight: 700, color: "var(--color-primary)", marginBottom: "1rem" }}
        >
          ROI Calculator
        </h2>
        <p>TODO — Calculate Your Savings (Phase 04)</p>
      </div>
    </section>
  );
}
